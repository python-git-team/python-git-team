# 결정

- 브랜치는 사용자 지정값인 `feature/list-utils`를 유지한다.
- 팀 미션의 예시 구현인 `dict.fromkeys()`를 그대로 사용한다.
- 함수 범위는 해시 가능한 리스트 항목으로 제한하며 중첩 리스트처럼 해시 불가능한 값의 추가 처리는 이번 작업에 포함하지 않는다.
- 테스트 전용 파일은 만들지 않고 직접 실행 예시와 인라인 assertion으로 필수 동작을 검증한다.
- 구현 커밋 메시지는 `feat: add list utility`를 사용한다.
